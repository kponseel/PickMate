// "Le plus susceptible" — secret vote game.
// Each round shows a "Qui est le plus susceptible de…" prompt; everyone votes
// for a target player. Reveal shows the tally; no scoring (it's a social game).
#pragma once

#include "game_api.h"

extern const Game MOST_LIKELY_GAME;
