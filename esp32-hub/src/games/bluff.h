// Bluff — trivia where the room types fake answers and tries to pick the
// real one out of a shuffled pool. Score for picking truth + score for
// fooling others.
#pragma once

#include "game_api.h"

extern const Game BLUFF_GAME;
